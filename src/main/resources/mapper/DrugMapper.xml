<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.educational.demo.dao.DrugMapper">
    <resultMap id="tableResultMap" type="com.educational.demo.model.Drugdictionary">
        <result property="drugId" column="drugId"/>
        <result property="drugName" column="drugName"/>
        <result property="sellingPrice" column="sellingPrice"/>
        <result property="createTime" column="createTime"/>
        <result property="updateTime" column="updateTime"/>
        <association property="units" column="com.educational.demo.model.Units">
            <result property="unitsId" column="unitsId"/>
            <result property="unitsName" column="unitsName"/>
        </association>
    </resultMap>

    <select id="listTableByPage" resultMap="tableResultMap">
        select dg.drugId, dg.drugName, dg.sellingPrice, dg.createTime, dg.updateTime, u.unitsName
        from drugdictionary dg
        left join units u on dg.unitsId = u.unitsId
        <if test="ew != null and ew.emptyOfWhere == false">
            ${ew.customSqlSegment}
        </if>
        order by  dg.drugId
    </select>


</mapper>