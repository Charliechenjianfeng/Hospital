<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.educational.demo.dao.RegistrationTypeMapper">

    <resultMap id="tableResultMap" type="com.educational.demo.model.RegistrationType">
        <result property="typeId" column="typeId"/>
        <result property="type" column="type"/>
        <result property="price" column="price"/>
        <result property="updateTime" column="updateTime"/>
        <result property="createTime" column="createTime"/>
    </resultMap>

    <select id="listTableByPage" resultMap="tableResultMap">
        select rt.typeId, rt.type,rt.price, rt.createTime, rt.updateTime from registrationtype rt
        <if test="ew != null and ew.emptyOfWhere == false">
            ${ew.customSqlSegment}
        </if>
        group by rt.typeId
    </select>

    <select id="listAll" resultType="com.educational.demo.model.RegistrationType">
        select rt.typeId,rt.type from registrationtype rt
    </select>

    <select id="listPriceById" resultType="Double">
        select rt.price from registrationtype rt where rt.typeId = #{id}
    </select>

</mapper>