<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.educational.demo.dao.CategoryMapper">
    <select id="listByArticleCount" resultType="com.educational.demo.model.Category">
        select tc.id, tc.name, tc.color, count(ta.id) articleCount
        from t_category tc
        left join t_article ta
        on tc.id = ta.categoryId
        where tc.display = 1
        group by tc.id
    </select>
    <select id="listTableByPage" resultType="com.educational.demo.model.Category"
            parameterType="com.baomidou.mybatisplus.extension.plugins.pagination.Page">
        select tc.id, tc.name, tc.display, tc.introduction, tc.color, tc.createTime, tc.updateTime, count(ta.id)
        articleCount
        from t_category tc
        left join t_article ta
        on tc.id = ta.categoryId
        <if test="ew != null and ew.emptyOfWhere == false">
            ${ew.customSqlSegment}
        </if>
        group by tc.id
    </select>
</mapper>